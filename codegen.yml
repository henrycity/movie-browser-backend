schema: "http://localhost:4000"
generates:
  src/graphql-version/graphql-types.ts:
    config:
      useIndexSignature: true
      mappers:
        List: ./models/list#List
    plugins:
      - typescript
      - typescript-resolvers